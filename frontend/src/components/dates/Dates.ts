export type Dates = {
    _id?: string,
    idea: string,
    location: string,
    description: string;
}

export const DefaultDates: Dates = {
    _id: undefined,
    idea: '',
    location: '',
    description: '',
}