'use client'

import React from "react";
import UpdateDateInfo from "@/components/dates/UpdateDate";

export default function foodDetails() {
    return(
        <main>
            <UpdateDateInfo/>
        </main>
    );
}